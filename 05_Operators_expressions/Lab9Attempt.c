# include <stdio.h>
# include <math.h>

int main(void)
{
int a = 0;
int b = 0;
int c = 0;
printf("Type the length of the the first side/ side A: ");
scanf("%d", a);
printf("Type the length of the the first side/ side B: ");
scanf("%d", b);
a^2 + b^2 = c;
double sqrt(double c);
printf("The sides of the triangle measure: %d %d %d \n", a, b, c);
}