#include<stdlib.h>
#include<stdio.h>

int main(int argc, char **argv){

	double a, b, h:

	printf("Enter the the length of the first side: ");
	scanf("%lf", &a);

	printf("Enter the the length of the first side: ");
	scanf("%lf", &b);

	h = sqrt(a*a + b*b):

	printf("Length of hypotenuse: %f\n", h):

	return 0;

>>>link the math header file "gcc pyth.c -lm"

